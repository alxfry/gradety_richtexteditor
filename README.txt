This is version 1.0.0
Known issues:
- On Google Chrome the preview buttons can only display the classes but not the tag of the style format. When using the usual style buttons the selection collapses and the first node in the text content will be formatted instead.